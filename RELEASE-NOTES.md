# 0.3.3-dev.14 — controlled OAuth candidate

The separate production endpoint passed native Codex login and a fresh-process check of content status, exact conversion, sourced explanation and mainnet address lookup on September 17, 2026. Content matched the existing published snapshot with no fallback. Access is explicitly allowlisted; sign-in alone does not grant access. The service is read-only. Testnet11 support retains its documented coverage limits; this production smoke used mainnet.

Existing staging lifecycle and quota evidence remains applicable to the shared controls. No new deletion or performance campaign was run. The new Worker ceiling is 250 ms; the earlier 100 ms optimization target stays deferred. CNI logo permission is pending; no CNI artwork is supplied. Public repository publication and vendor approval are separate.
